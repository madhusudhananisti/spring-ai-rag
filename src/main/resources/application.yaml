spring:
  application:
    name: spring-ai-rag
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      embedding:
        options:
          model: text-embedding-3-small
    chat:
      options:
        model: gpt-4-turbo



sfg:
  aiapp:
    vectorStorePath: E:\\Code\\vectorstore.json
    documentsToLoad:
      - classpath:/movies500.csv
